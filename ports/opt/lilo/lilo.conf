#
# /etc/lilo.conf: lilo(8) configuration, see lilo.conf(5)
#

lba32
install=text
boot=/dev/#DEVICE#
image=/boot/vmlinuz
        label=CRUX
        read-only
        append="root=/dev/#ROOT_DEVICE#"

# End of file
